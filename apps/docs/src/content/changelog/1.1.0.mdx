export const description = "Adds soft and ghost Button variants and per-role color schemes.";

# 1.1.0

## Added

- `soft` and `ghost` Button variants.
- Per-role color schemes (`info`, `warning`) across components.
