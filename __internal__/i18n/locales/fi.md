# `MESSAGES_FI` — Finnish catalog

Built-in **Finnish** catalog, selected when the active locale's primary subtag is `fi`. Shape + key
list: `../messages.md`. Selection + fallback: `catalogs.md`.

- Both count-bearing keys: `1` takes the nominative; any other count takes the partitive singular —
  `1 päivämäärä valittu` / `2 päivämäärää valittu`, `1 vaihtoehto saatavilla` /
  `2 vaihtoehtoa saatavilla`.
- Ranges use an en dash (`{{start}}–{{end}}`), the Finnish range separator.

<!-- no-rejected-alternatives: catalog data — the partitive form and the en-dash range
separator are Finnish orthography; the contract shape they fill is decided in `../messages.md`. -->
