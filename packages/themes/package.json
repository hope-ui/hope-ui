{
  "name": "@hope-ui/themes",
  "version": "0.0.0",
  "description": "hope-ui themes — one tree-shakable package, per-theme subpaths (@hope-ui/themes/hope is the default visual identity). Each theme ships a Tailwind v4 CSS file (design tokens as --hope-* CSS variables + an @theme inline mapping to clean utilities); import it into your Tailwind entry. No JS config, no codegen.",
  "type": "module",
  "sideEffects": false,
  "files": ["src"],
  "exports": {
    "./hope": {
      "style": "./src/hope/theme.css",
      "default": "./src/hope/theme.css"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "peerDependencies": {
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "@hope-ui/theming": "workspace:*",
    "@types/node": "^22.0.0"
  }
}
